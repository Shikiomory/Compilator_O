MODULE Test;
  IMPORT Out;
  VAR x, y: INTEGER;
BEGIN
  x := 6;     (* 0110 *)
  y := 3;     (* 0011 *)

  IF x = 6 & (y) = 3 THEN   (* 0110 & 0011 = 0010 => 2 *)
    Out.Int(999, 5);    (* Это выполнится *)
  ELSE
    Out.Int(0, 5);      (* Это не выполнится *)
  END;

  Out.Ln
END Test.
